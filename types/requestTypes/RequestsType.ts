export default interface RequestsType {
  _id?: string;
  request_id: string;
  user_id: string;
  product_id: string;
  username: string;
  name: string;
  category: string;
  quantity: number;
  status: string;
}
